import { AppRegistry } from 'react-native';
import App from './App'; // Ou o nome do componente principal do seu aplicativo

// Defina o nome do aplicativo
const appName = "gestao-de-tarefas";

// Registre o componente principal do aplicativo
AppRegistry.registerComponent(appName, () => App);
